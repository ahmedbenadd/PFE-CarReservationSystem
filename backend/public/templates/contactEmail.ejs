<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Confirmation - AutoGO</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: <%= primaryColor %>;
            color: #ffffff;
            text-align: center;
            padding: 20px 0;
        }
        .header img {
            max-width: 150px;
        }
        .content {
            padding: 20px;
            text-align: center;
            line-height: 1.6;
        }
        .content p {
            font-size: 16px;
            color: #333333;
            margin: 10px 0;
        }
        .footer {
            text-align: center;
            padding: 15px;
            background-color: #f5f5f5;
            font-size: 14px;
            color: #888888;
        }
        .footer a {
            color: <%= primaryColor %>;
            text-decoration: none;
        }
        .footer a:hover {
            text-decoration: underline;
        }
        @media (max-width: 600px) {
            .header img {
                max-width: 100px;
            }
            .content p {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
<div class="email-container">
    <!-- Header -->
    <div class="header">
        <img src="https://i.imgur.com/y1WYldj.png" alt="Logo">
    </div>

    <!-- Content -->
    <div class="content">
        <p>Hi <%= name %>,</p>
        <p>Thank you for reaching out to us at AutoGO! We have received your message:</p>
        <p><strong>"<%= message %>"</strong></p>
        <p>We will get back to you as soon as possible. In the meantime, feel free to explore our platform:</p>
        <p>
            <a href="<%= websiteUrl %>" style="display: inline-block; padding: 10px 20px; background-color: <%= primaryColor %>; color: #fff; text-decoration: none; border-radius: 5px;">
                Visit AutoGO
            </a>
        </p>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>&copy; <%= new Date().getFullYear() %> <a href="<%= websiteUrl %>">AutoGO</a>. All rights reserved.</p>
    </div>
</div>
</body>
</html>